public String decodeString(ArrayList<StringPart>parts){
  for(int i=0;i<parts.size();i++){
    String a="";
    int a = parts.getStart(i);
    int b = parts.getLength(i);
    String a += new String(masterString.substring(a,a+b));
    }
    return a;
 }
  
